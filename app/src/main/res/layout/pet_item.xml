<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/pet_image"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/petnames_background"
        android:layout_width="146dp"
        android:layout_height="99dp"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="4dp"
        android:background="@drawable/petname_background"
        app:layout_constraintBottom_toBottomOf="@id/pet_image"
        app:layout_constraintStart_toStartOf="@id/pet_image" />

    <TextView
        android:id="@+id/pet_Name"
        android:layout_width="127dp"
        android:layout_height="35dp"
        android:layout_marginStart="36dp"
        android:maxLines="2"
        android:textColor="@color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/petnames_background"
        app:layout_constraintStart_toStartOf="@id/petnames_background"
        app:layout_constraintTop_toTopOf="@id/petnames_background"
        app:layout_constraintVertical_bias="0.477"
        tools:text="PetName" />

    <TextView
        android:id="@+id/pet_Text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/pet_Name"
        app:layout_constraintStart_toStartOf="@id/pet_Name"
        tools:text="Description"
        android:textSize="16sp"
        android:layout_marginTop="5dp"
        android:visibility="gone"/>





</androidx.constraintlayout.widget.ConstraintLayout>